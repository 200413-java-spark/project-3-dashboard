document.addEventListener("DOMContentLoaded", function () {
    tail.select("select", {
        /* Your Options */
        
        search: true,
        multiLimit: 5,
        hideSelected: true,
        hideDisabled: true,
        multiShowCount: false,
        multiContainer: true
    });
});
